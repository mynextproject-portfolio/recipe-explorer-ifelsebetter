# Prometheus scrape configuration for Recipe Explorer.
# Prometheus pulls metrics from the /metrics endpoint exposed by the app.

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'recipe-explorer'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['app:8000']
